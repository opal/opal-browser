2048 clone with Opal-Browser
============================

A 167 line implementation of 2048, but in Ruby inside a browser!

To install, run:

    $ bundle install

Start server
------------

    $ bundle exec rackup